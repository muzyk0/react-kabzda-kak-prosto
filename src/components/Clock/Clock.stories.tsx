import React, {useEffect, useState} from 'react';
import {Clock} from './Clock';

export default {
    title: 'clock',
    component: 'Clock'
}

export const ClockExample = () => {

    return <Clock/>
}